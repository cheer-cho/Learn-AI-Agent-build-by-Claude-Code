"""Deployable applications built on top of the ``techcorp_agent`` library.

This namespace holds the *services* — as opposed to the library in
``src/techcorp_agent/`` and the teaching modules in ``course/``. Module 21 adds
``apps.api``: the FastAPI service that turns the capstone agent into a monitored,
deployable HTTP endpoint.
"""
